
1) Open database -> docker exec -it cassandra cqlsh

2) Create keyspace ->
CREATE KEYSPACE IF NOT EXISTS ecommerce
WITH replication = {'class': 'SimpleStrategy', 'replication_factor': 1};

USE ecommerce;

3) Create Inventory ->
CREATE TABLE IF NOT EXISTS inventory_items (
    product_id text PRIMARY KEY,
    id text,
    stock_quantity int,
    last_updated timestamp
);

4) Create Reserved ->
CREATE TABLE IF NOT EXISTS reserved_stock (
    product_id text,
    cart_id text,
    quantity int,
    PRIMARY KEY (product_id, cart_id)
);

5) Create Cart Items ->
CREATE TABLE IF NOT EXISTS cart_items (
    cart_id text,
    product_id text,
    id text,
    quantity int,
    PRIMARY KEY (cart_id, product_id)
);

6) Added some data ->
INSERT INTO inventory_items (product_id, id, stock_quantity, last_updated)
VALUES ('98b7f8ae-15bc-11f1-82eb-2cfda1bbb0fd', 'item-1', 100, toTimestamp(now()));

